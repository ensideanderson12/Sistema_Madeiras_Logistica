/** * IA de Otimização - Sistema Madeiras Logística * Algoritmos de machine learning para otimização de rotas e custos */ class IAOptimization {constructor() {this.modelData = {routeOptimization: [],costPrediction: [],demandForecasting: [],priceOptimization: [] };this.trainingHistory = [];this.predictions = new Map();this.initializeAI();this.loadHistoricalData();}/** * Inicializa os módulos de IA */ initializeAI() {console.log('[IA] Inicializando módulos de inteligência artificial...');// Configurações dos algoritmos this.config = {routeOptimization: {algorithm: 'genetic',populationSize: 50,generations: 100,mutationRate: 0.1,crossoverRate: 0.8 },costPrediction: {algorithm: 'regression',features: ['distance','fuelPrice','weight','season','dayOfWeek'],windowSize: 30,learningRate: 0.01 },demandForecasting: {algorithm: 'timeSeries',seasonality: 'weekly',trend: 'linear',forecastHorizon: 30 }};this.setupNeuralNetworks();this.initializeOptimizationAlgorithms();}/** * Configura redes neurais simplificadas */ setupNeuralNetworks() {// Rede neural para predição de custos this.costPredictionNetwork = {inputLayer: 5,// features de entrada hiddenLayer: 10,outputLayer: 1,// custo previsto weights: this.initializeWeights(5,10,1),biases: this.initializeBiases(10,1) };// Rede neural para otimização de preços this.priceOptimizationNetwork = {inputLayer: 8,// market factors hiddenLayer: 15,outputLayer: 3,// optimal prices for different scenarios weights: this.initializeWeights(8,15,3),biases: this.initializeBiases(15,3) };}/** * Inicializa pesos aleatórios para redes neurais */ initializeWeights(inputSize,hiddenSize,outputSize) {return {inputToHidden: this.randomMatrix(inputSize,hiddenSize),hiddenToOutput: this.randomMatrix(hiddenSize,outputSize) };}/** * Inicializa biases para redes neurais */ initializeBiases(hiddenSize,outputSize) {return {hidden: this.randomArray(hiddenSize),output: this.randomArray(outputSize) };}/** * Cria matriz aleatória */ randomMatrix(rows,cols) {const matrix = [];for (let i = 0;i < rows;i++) {matrix[i] = [];for (let j = 0;j < cols;j++) {matrix[i][j] = (Math.random() - 0.5) * 2;// Entre -1 e 1 }}return matrix;}/** * Cria array aleatório */ randomArray(size) {return Array.from({length: size },() => (Math.random() - 0.5) * 2);}/** * Inicializa algoritmos de otimização */ initializeOptimizationAlgorithms() {// Algoritmo genético para otimização de rotas this.geneticAlgorithm = {population: [],fitness: [],bestSolution: null,generation: 0 };// Algoritmo de otimização por enxame de partículas this.particleSwarm = {particles: [],globalBest: null,iteration: 0 };}/** * Carrega dados históricos para treinamento */ loadHistoricalData() {const historicalData = localStorage.getItem('iaHistoricalData');if (historicalData) {this.modelData = JSON.parse(historicalData);}else {this.generateSyntheticData();}this.trainModels();}/** * Gera dados sintéticos para treinamento inicial */ generateSyntheticData() {console.log('[IA] Gerando dados sintéticos para treinamento...');// Gerar dados de rotas for (let i = 0;i < 1000;i++) {this.modelData.routeOptimization.push(this.generateRouteData());}// Gerar dados de custos for (let i = 0;i < 1000;i++) {this.modelData.costPrediction.push(this.generateCostData());}// Gerar dados de demanda for (let i = 0;i < 365;i++) {this.modelData.demandForecasting.push(this.generateDemandData(i));}this.saveModelData();}/** * Gera dados sintéticos de rota */ generateRouteData() {const origin = this.randomCoordinate();const destination = this.randomCoordinate();const distance = this.calculateDistance(origin,destination);const optimalRoute = this.generateOptimalRoute(origin,destination);return {origin,destination,distance,optimalRoute,estimatedTime: distance / 80,// Velocidade média 80 km/h fuelConsumption: distance / 3.5,cost: this.calculateRouteCost(distance),trafficFactor: Math.random() * 0.3 + 0.8,// 0.8 a 1.1 roadQuality: Math.random() * 0.2 + 0.8 // 0.8 a 1.0 };}/** * Gera coordenada aleatória (Brasil) */ randomCoordinate() {return {lat: -33.75 + Math.random() * 28.5,// Entre -33.75 e -5.25 (Brasil) lng: -73.98 + Math.random() * 39.58 // Entre -73.98 e -34.4 (Brasil) };}/** * Calcula distância entre dois pontos */ calculateDistance(point1,point2) {const R = 6371;// Raio da Terra em km const dLat = this.toRadians(point2.lat - point1.lat);const dLng = this.toRadians(point2.lng - point1.lng);const a = Math.sin(dLat/2) * Math.sin(dLat/2) + Math.cos(this.toRadians(point1.lat)) * Math.cos(this.toRadians(point2.lat)) * Math.sin(dLng/2) * Math.sin(dLng/2);const c = 2 * Math.atan2(Math.sqrt(a),Math.sqrt(1-a));return R * c;}/** * Converte graus para radianos */ toRadians(degrees) {return degrees * (Math.PI / 180);}/** * Gera rota otimizada (simulada) */ generateOptimalRoute(origin,destination) {// Simula pontos intermediários para rota otimizada const intermediatePoints = [];const numPoints = Math.floor(Math.random() * 3) + 1;for (let i = 0;i < numPoints;i++) {intermediatePoints.push({lat: origin.lat + (destination.lat - origin.lat) * (i + 1) / (numPoints + 1),lng: origin.lng + (destination.lng - origin.lng) * (i + 1) / (numPoints + 1) });}return [origin,...intermediatePoints,destination];}/** * Calcula custo da rota */ calculateRouteCost(distance) {const fuelCost = (distance / 3.5) * 5.5;// 3.5 km/l,R$ 5.50/l const driverCost = Math.ceil(distance / 640) * 250;// R$ 250/dia,640 km/dia const tollCost = distance * 0.15;const maintenanceCost = distance * 0.25;return fuelCost + driverCost + tollCost + maintenanceCost;}/** * Gera dados sintéticos de custo */ generateCostData() {const distance = Math.random() * 2000 + 100;// 100 a 2100 km const fuelPrice = Math.random() * 2 + 4.5;// R$ 4.50 a R$ 6.50 const weight = Math.random() * 40 + 5;// 5 a 45 toneladas const season = Math.floor(Math.random() * 4);// 0-3 (estações) const dayOfWeek = Math.floor(Math.random() * 7);// 0-6 const baseCost = this.calculateRouteCost(distance);const seasonMultiplier = [1.0,1.1,0.95,1.05][season];const dayMultiplier = dayOfWeek < 5 ? 1.0 : 1.15;// Weekend premium const weightMultiplier = 1 + (weight - 25) * 0.01;// Weight impact const actualCost = baseCost * seasonMultiplier * dayMultiplier * weightMultiplier;return {features: [distance,fuelPrice,weight,season,dayOfWeek],cost: actualCost,timestamp: Date.now() - Math.random() * 365 * 24 * 60 * 60 * 1000 };}/** * Gera dados sintéticos de demanda */ generateDemandData(dayIndex) {const basedemand = 100;const seasonalFactor = Math.sin(dayIndex * 2 * Math.PI / 365) * 20;const weeklyFactor = Math.sin(dayIndex * 2 * Math.PI / 7) * 10;const randomFactor = (Math.random() - 0.5) * 15;const demand = Math.max(0,basedemand + seasonalFactor + weeklyFactor + randomFactor);return {day: dayIndex,demand: Math.round(demand),timestamp: Date.now() - (365 - dayIndex) * 24 * 60 * 60 * 1000 };}/** * Treina todos os modelos */ trainModels() {console.log('[IA] Iniciando treinamento dos modelos...');this.trainCostPredictionModel();this.trainRouteOptimizationModel();this.trainDemandForecastingModel();console.log('[IA] Treinamento concluído');}/** * Treina modelo de predição de custos */ trainCostPredictionModel() {const data = this.modelData.costPrediction;if (data.length === 0) return;console.log('[IA] Treinando modelo de predição de custos...');// Normalizar dados const normalizedData = this.normalizeData(data);// Treinamento simplificado da rede neural for (let epoch = 0;epoch < 100;epoch++) {let totalError = 0;for (const sample of normalizedData) {const prediction = this.forwardPass(sample.features,this.costPredictionNetwork);const error = sample.cost - prediction[0];totalError += error * error;// Backpropagation simplificada this.updateWeights(sample.features,error,this.costPredictionNetwork);}if (epoch % 20 === 0) {console.log(`[IA] Época ${epoch},Erro: ${Math.sqrt(totalError / data.length).toFixed(4)}`);}}}/** * Normaliza dados para treinamento */ normalizeData(data) {const features = data.map(d => d.features);const costs = data.map(d => d.cost);// Min-Max normalization const featureStats = this.calculateFeatureStats(features);const costStats = {min: Math.min(...costs),max: Math.max(...costs) };return data.map(d => ({features: d.features.map((f,i) => (f - featureStats[i].min) / (featureStats[i].max - featureStats[i].min) ),cost: (d.cost - costStats.min) / (costStats.max - costStats.min) }));}/** * Calcula estatísticas das features */ calculateFeatureStats(features) {const numFeatures = features[0].length;const stats = [];for (let i = 0;i < numFeatures;i++) {const values = features.map(f => f[i]);stats.push({min: Math.min(...values),max: Math.max(...values),mean: values.reduce((a,b) => a + b) / values.length });}return stats;}/** * Forward pass da rede neural */ forwardPass(inputs,network) {// Input para hidden layer const hiddenValues = [];for (let i = 0;i < network.hiddenLayer;i++) {let sum = network.biases.hidden[i];for (let j = 0;j < inputs.length;j++) {sum += inputs[j] * network.weights.inputToHidden[j][i];}hiddenValues[i] = this.sigmoid(sum);}// Hidden para output layer const outputs = [];for (let i = 0;i < network.outputLayer;i++) {let sum = network.biases.output[i];for (let j = 0;j < hiddenValues.length;j++) {sum += hiddenValues[j] * network.weights.hiddenToOutput[j][i];}outputs[i] = this.sigmoid(sum);}return outputs;}/** * Função de ativação sigmoid */ sigmoid(x) {return 1 / (1 + Math.exp(-x));}/** * Atualiza pesos da rede neural (backpropagation simplificada) */ updateWeights(inputs,error,network) {const learningRate = this.config.costPrediction.learningRate;// Atualização simplificada - apenas para demonstração for (let i = 0;i < network.weights.inputToHidden.length;i++) {for (let j = 0;j < network.weights.inputToHidden[i].length;j++) {network.weights.inputToHidden[i][j] += learningRate * error * inputs[i];}}}/** * Treina modelo de otimização de rotas */ trainRouteOptimizationModel() {console.log('[IA] Treinando modelo de otimização de rotas...');// Implementação do algoritmo genético this.initializeGeneticPopulation();for (let generation = 0;generation < this.config.routeOptimization.generations;generation++) {this.evaluatePopulation();this.selectAndReproduce();this.mutatePopulation();if (generation % 20 === 0) {const bestFitness = Math.max(...this.geneticAlgorithm.fitness);console.log(`[IA] Geração ${generation},Melhor fitness: ${bestFitness.toFixed(4)}`);}}}/** * Inicializa população para algoritmo genético */ initializeGeneticPopulation() {this.geneticAlgorithm.population = [];for (let i = 0;i < this.config.routeOptimization.populationSize;i++) {// Cada indivíduo representa parâmetros de otimização de rota const individual = {speedFactor: Math.random() * 0.4 + 0.8,// 0.8 - 1.2 fuelEfficiency: Math.random() * 0.3 + 0.85,// 0.85 - 1.15 routePreference: Math.random(),// 0 - 1 (highway vs local) tollAvoidance: Math.random() * 0.5,// 0 - 0.5 timeWeight: Math.random(),// 0 - 1 costWeight: 1 - Math.random() // complemento do timeWeight };this.geneticAlgorithm.population.push(individual);}}/** * Avalia população do algoritmo genético */ evaluatePopulation() {this.geneticAlgorithm.fitness = this.geneticAlgorithm.population.map(individual => {return this.evaluateRouteOptimization(individual);});}/** * Avalia qualidade de um indivíduo para otimização de rota */ evaluateRouteOptimization(individual) {// Simula avaliação baseada em múltiplas rotas let totalFitness = 0;const numRoutes = 10;for (let i = 0;i < numRoutes;i++) {const route = this.modelData.routeOptimization[ Math.floor(Math.random() * this.modelData.routeOptimization.length) ];const optimizedCost = this.calculateOptimizedCost(route,individual);const originalCost = route.cost;// Fitness é a economia percentual const fitness = Math.max(0,(originalCost - optimizedCost) / originalCost);totalFitness += fitness;}return totalFitness / numRoutes;}/** * Calcula custo otimizado para uma rota */ calculateOptimizedCost(route,individual) {const baseCost = route.cost;// Aplica otimizações baseadas no indivíduo const speedOptimization = 1 - (individual.speedFactor - 1) * 0.1;const fuelOptimization = individual.fuelEfficiency;const routeOptimization = 1 - individual.routePreference * 0.05;const tollOptimization = 1 - individual.tollAvoidance * 0.15;return baseCost * speedOptimization * fuelOptimization * routeOptimization * tollOptimization;}/** * Seleção e reprodução no algoritmo genético */ selectAndReproduce() {const newPopulation = [];const populationSize = this.geneticAlgorithm.population.length;// Elitismo - mantém os melhores 10% const eliteSize = Math.floor(populationSize * 0.1);const sortedIndices = this.geneticAlgorithm.fitness .map((fitness,index) => ({fitness,index })) .sort((a,b) => b.fitness - a.fitness) .map(item => item.index);for (let i = 0;i < eliteSize;i++) {newPopulation.push({...this.geneticAlgorithm.population[sortedIndices[i]]});}// Reprodução while (newPopulation.length < populationSize) {const parent1 = this.selectParent();const parent2 = this.selectParent();const offspring = this.crossover(parent1,parent2);newPopulation.push(offspring);}this.geneticAlgorithm.population = newPopulation;}/** * Seleção de pais (tournament selection) */ selectParent() {const tournamentSize = 5;let bestIndex = Math.floor(Math.random() * this.geneticAlgorithm.population.length);for (let i = 1;i < tournamentSize;i++) {const candidateIndex = Math.floor(Math.random() * this.geneticAlgorithm.population.length);if (this.geneticAlgorithm.fitness[candidateIndex] > this.geneticAlgorithm.fitness[bestIndex]) {bestIndex = candidateIndex;}}return this.geneticAlgorithm.population[bestIndex];}/** * Crossover entre dois pais */ crossover(parent1,parent2) {const offspring = {};const keys = Object.keys(parent1);for (const key of keys) {// Crossover uniforme offspring[key] = Math.random() < this.config.routeOptimization.crossoverRate ? parent1[key] : parent2[key];}return offspring;}/** * Mutação da população */ mutatePopulation() {for (const individual of this.geneticAlgorithm.population) {if (Math.random() < this.config.routeOptimization.mutationRate) {this.mutateIndividual(individual);}}}/** * Mutação de um indivíduo */ mutateIndividual(individual) {const keys = Object.keys(individual);const keyToMutate = keys[Math.floor(Math.random() * keys.length)];// Mutação gaussiana const mutationStrength = 0.1;const mutation = (Math.random() - 0.5) * mutationStrength;individual[keyToMutate] = Math.max(0,Math.min(1,individual[keyToMutate] + mutation));}/** * Treina modelo de previsão de demanda */ trainDemandForecastingModel() {console.log('[IA] Treinando modelo de previsão de demanda...');const data = this.modelData.demandForecasting;if (data.length === 0) return;// Decomposição de séries temporais simples this.demandModel = {trend: this.calculateTrend(data),seasonality: this.calculateSeasonality(data),residuals: this.calculateResiduals(data) };}/** * Calcula tendência da demanda */ calculateTrend(data) {const x = data.map((_,i) => i);const y = data.map(d => d.demand);return this.linearRegression(x,y);}/** * Regressão linear simples */ linearRegression(x,y) {const n = x.length;const sumX = x.reduce((a,b) => a + b,0);const sumY = y.reduce((a,b) => a + b,0);const sumXY = x.reduce((sum,xi,i) => sum + xi * y[i],0);const sumXX = x.reduce((sum,xi) => sum + xi * xi,0);const slope = (n * sumXY - sumX * sumY) / (n * sumXX - sumX * sumX);const intercept = (sumY - slope * sumX) / n;return {slope,intercept };}/** * Calcula sazonalidade */ calculateSeasonality(data) {const seasonalPeriod = 7;// Semanal const seasonal = new Array(seasonalPeriod).fill(0);const counts = new Array(seasonalPeriod).fill(0);data.forEach((d,i) => {const seasonIndex = i % seasonalPeriod;seasonal[seasonIndex] += d.demand;counts[seasonIndex]++;});return seasonal.map((sum,i) => counts[i] > 0 ? sum / counts[i] : 0);}/** * Calcula resíduos */ calculateResiduals(data) {return data.map((d,i) => {const trendValue = this.demandModel?.trend?.slope * i + this.demandModel?.trend?.intercept || d.demand;const seasonalValue = this.demandModel?.seasonality?.[i % 7] || 0;return d.demand - trendValue - seasonalValue;});}/** * Prediz custo para novos parâmetros */ predictCost(distance,fuelPrice,weight,season = 0,dayOfWeek = 1) {const features = [distance,fuelPrice,weight,season,dayOfWeek];// Normalizar features (usando estatísticas armazenadas) const normalizedFeatures = features.map((f,i) => {// Normalização simplificada - em produção usaria estatísticas reais const ranges = [2000,3,40,3,6];// Ranges aproximados return f / ranges[i];});const prediction = this.forwardPass(normalizedFeatures,this.costPredictionNetwork);// Desnormalizar resultado const maxCost = 15000;// Custo máximo aproximado return prediction[0] * maxCost;}/** * Otimiza rota usando o melhor indivíduo encontrado */ optimizeRoute(origin,destination,preferences = {}) {const bestIndividual = this.getBestIndividual();// Simula otimização de rota const baseRoute = this.generateOptimalRoute(origin,destination);const distance = this.calculateDistance(origin,destination);const optimizedRoute = {path: baseRoute,distance: distance,estimatedTime: distance / (80 * bestIndividual.speedFactor),estimatedCost: this.calculateOptimizedCost( {distance,cost: this.calculateRouteCost(distance) },bestIndividual ),optimizations: {fuelSaving: (1 - bestIndividual.fuelEfficiency) * 100,timeSaving: (bestIndividual.speedFactor - 1) * 100,costSaving: (1 - bestIndividual.routePreference) * 5 }};return optimizedRoute;}/** * Obtém o melhor indivíduo do algoritmo genético */ getBestIndividual() {if (this.geneticAlgorithm.fitness.length === 0) {// Retorna valores padrão se não houve treinamento return {speedFactor: 1.0,fuelEfficiency: 1.0,routePreference: 0.5,tollAvoidance: 0.3,timeWeight: 0.5,costWeight: 0.5 };}const bestIndex = this.geneticAlgorithm.fitness.indexOf(Math.max(...this.geneticAlgorithm.fitness));return this.geneticAlgorithm.population[bestIndex];}/** * Prevê demanda futura */ forecastDemand(daysAhead = 30) {if (!this.demandModel) return [];const forecast = [];const lastDayIndex = this.modelData.demandForecasting.length;for (let i = 0;i < daysAhead;i++) {const dayIndex = lastDayIndex + i;const trendValue = this.demandModel.trend.slope * dayIndex + this.demandModel.trend.intercept;const seasonalValue = this.demandModel.seasonality[dayIndex % 7];const forecastValue = Math.max(0,trendValue + seasonalValue);forecast.push({day: dayIndex,demand: Math.round(forecastValue),confidence: Math.max(0.6,1 - (i / daysAhead) * 0.4) // Confiança diminui com distância });}return forecast;}/** * Analisa padrões de mercado */ analyzeMarketPatterns() {const analysis = {costTrends: this.analyzeCostTrends(),demandPatterns: this.analyzeDemandPatterns(),optimizationOpportunities: this.identifyOptimizationOpportunities(),riskFactors: this.identifyRiskFactors() };return analysis;}/** * Analisa tendências de custo */ analyzeCostTrends() {const costs = this.modelData.costPrediction.map(d => d.cost);const recent = costs.slice(-30);const previous = costs.slice(-60,-30);const recentAvg = recent.reduce((a,b) => a + b,0) / recent.length;const previousAvg = previous.reduce((a,b) => a + b,0) / previous.length;return {currentAverage: recentAvg,trend: recentAvg > previousAvg ? 'increasing' : 'decreasing',changePercent: ((recentAvg - previousAvg) / previousAvg) * 100,volatility: this.calculateVolatility(recent) };}/** * Calcula volatilidade */ calculateVolatility(values) {const mean = values.reduce((a,b) => a + b,0) / values.length;const variance = values.reduce((sum,value) => sum + Math.pow(value - mean,2),0) / values.length;return Math.sqrt(variance);}/** * Analisa padrões de demanda */ analyzeDemandPatterns() {const demands = this.modelData.demandForecasting.map(d => d.demand);return {averageDemand: demands.reduce((a,b) => a + b,0) / demands.length,peakDemand: Math.max(...demands),lowDemand: Math.min(...demands),seasonalStrength: this.calculateSeasonalStrength(),growth: this.demandModel?.trend?.slope || 0 };}/** * Calcula força sazonal */ calculateSeasonalStrength() {if (!this.demandModel?.seasonality) return 0;const seasonal = this.demandModel.seasonality;const mean = seasonal.reduce((a,b) => a + b,0) / seasonal.length;const maxDeviation = Math.max(...seasonal.map(s => Math.abs(s - mean)));return maxDeviation / mean;}/** * Identifica oportunidades de otimização */ identifyOptimizationOpportunities() {const opportunities = [];// Analisar custos de combustível const fuelCosts = this.modelData.costPrediction.map(d => d.features[1]);const avgFuelPrice = fuelCosts.reduce((a,b) => a + b,0) / fuelCosts.length;if (avgFuelPrice > 5.5) {opportunities.push({type: 'fuel_optimization',description: 'Preço de combustível elevado - considerar otimização de rotas',impact: 'high',savingPotential: 15 });}// Analisar utilização de capacidade const weights = this.modelData.costPrediction.map(d => d.features[2]);const avgWeight = weights.reduce((a,b) => a + b,0) / weights.length;if (avgWeight < 20) {opportunities.push({type: 'capacity_optimization',description: 'Baixa utilização de capacidade - consolidar cargas',impact: 'medium',savingPotential: 10 });}return opportunities;}/** * Identifica fatores de risco */ identifyRiskFactors() {const risks = [];// Volatilidade de custos const costVolatility = this.analyzeCostTrends().volatility;if (costVolatility > 1000) {risks.push({type: 'cost_volatility',description: 'Alta volatilidade nos custos operacionais',severity: 'medium',probability: 0.7 });}// Dependência de combustível const fuelDependency = this.calculateFuelDependency();if (fuelDependency > 0.4) {risks.push({type: 'fuel_dependency',description: 'Alta dependência de custos de combustível',severity: 'high',probability: 0.8 });}return risks;}/** * Calcula dependência de combustível */ calculateFuelDependency() {// Simula cálculo baseado na proporção de custos de combustível return 0.35;// 35% dos custos são combustível }/** * Salva dados do modelo */ saveModelData() {localStorage.setItem('iaHistoricalData',JSON.stringify(this.modelData));}/** * Adiciona novo ponto de dados para retreinamento */ addTrainingData(type,data) {if (this.modelData[type]) {this.modelData[type].push(data);// Manter apenas últimos 1000 pontos if (this.modelData[type].length > 1000) {this.modelData[type].shift();}this.saveModelData();}}/** * Retreina modelos com novos dados */ retrainModels() {console.log('[IA] Retreinando modelos com novos dados...');this.trainModels();}/** * Obtém estatísticas dos modelos */ getModelStats() {return {trainingDataSize: {routes: this.modelData.routeOptimization.length,costs: this.modelData.costPrediction.length,demand: this.modelData.demandForecasting.length },lastTraining: this.trainingHistory[this.trainingHistory.length - 1],accuracy: this.calculateModelAccuracy(),predictions: this.predictions.size };}/** * Calcula precisão dos modelos */ calculateModelAccuracy() {// Simulação de precisão - em produção seria calculada com dados de teste return {costPrediction: 0.85,routeOptimization: 0.78,demandForecasting: 0.82 };}}// Inicializar módulo de IA document.addEventListener('DOMContentLoaded',() => {console.log('[IA] Inicializando sistema de inteligência artificial...');window.iaOptimization = new IAOptimization();// Expor algumas funções globalmente para uso no dashboard window.predictRouteCost = (distance,fuelPrice,weight) => {return window.iaOptimization.predictCost(distance,fuelPrice,weight);};window.optimizeRoute = (origin,destination) => {return window.iaOptimization.optimizeRoute(origin,destination);};window.forecastDemand = (days = 30) => {return window.iaOptimization.forecastDemand(days);};window.analyzeMarket = () => {return window.iaOptimization.analyzeMarketPatterns();};});
